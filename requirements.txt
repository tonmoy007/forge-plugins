# Forge Plugin Development — Python Dependencies
#
# These are for SCRIPTS, not hooks. Hooks are stdlib-only by design (see ADR-001).
# Scripts can use these freely.

# python-frontmatter is TEST-ONLY as of v0.1.3.1. Production scripts parse
# frontmatter with stdlib + PyYAML so external users don't need to pip-install it.
# (Claude Code plugin installs don't trigger pip; and `pip install frontmatter`
# resolves to an unrelated package.) Two existing tests still import it; remove
# when those are rewritten.
python-frontmatter>=1.0.0

# Proposal schema validation in stop-reflect v4.1 helper modules
pydantic>=2.0.0

# YAML for gate-criteria.md, profile files, lessons.yaml
PyYAML>=6.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Optional: for richer terminal output in scripts (not required)
# rich>=13.0.0

# Optional: CLI framework if scripts get complex
# click>=8.1.0
